<?xml version="1.0" encoding="utf-8"?>
<!-- just_audio injects per-request headers (User-Agent for googlevideo)
     through a localhost proxy (http://127.0.0.1). Loopback cleartext is
     safe; everything else stays HTTPS-only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
